<h1>User Edit</h1>

<%= simple_form_for(:profil) do |f| %>
  <%= f.input :name, placeholder: @buddy.name %>
  <%= f.input :description, placeholder: @buddy.description %>
  <%= f.submit %>
<% end %>

<%= link_to "Back to buddies list MF", buddies_path(@buddy), class: "btn btn-primary" %>
<%= link_to "Mes prestas admin", admin_prestations_path, class: "btn btn-primary" %>
<%# <%= link_to "Mes réservations perso", prestation_bookings_path(prestation_id: prestation.id), class: "btn btn-primary" %>
